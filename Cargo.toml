[package]
name = "robots"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.release]
opt-level =  3
strip = true
panic = "abort"

[dependencies]
rand = "0.8.5"
crossterm = "0.22"
argh = "0.1.12"
